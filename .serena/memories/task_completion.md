For implementation tasks, validate with Docker pytest plus coverage, and Docker lint/type checks. Check HA integration structure against custom_components/abstractor/manifest.json and config flow behavior. Confirm changed files with git status/diff; preserve unrelated dirty changes and never mutate submodules. Current repository has no .github/workflows CI directory, so local validation is the effective gate.